"""Diagnostics panel: lists verifier output, click → jump to line.

``unsupported`` spans must be visually distinct from warnings and errors — the user has to be
able to see that a span was *not interpreted* rather than merely flagged.

Implemented in T3.4.
"""
